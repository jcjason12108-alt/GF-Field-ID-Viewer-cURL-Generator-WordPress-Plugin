# -GF-Field-ID-Viewer-cURL-Generator-WordPress-Plugin-
 View **field IDs** (including sub-IDs for compound fields like Name, Address, etc.).   2. Generate ready-to-run **cURL examples** (URL-encoded, JSON, and multipart for file uploads) for testing form submissions via the **Gravity Forms REST API**.  
